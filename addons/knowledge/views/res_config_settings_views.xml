<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.knowledge</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="62"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Kiến thức" string="Kiến thức" name="knowledge"
                     groups="knowledge.group_knowledge_manager">
                    <block title="Bài viết">
                        <setting string="Kho bài viết"
                                 help="Quyền được đặt trên bài viết gốc và áp dụng cho cả nhánh: sửa được, chỉ đọc, hoặc riêng tư.">
                            <button name="%(knowledge.knowledge_article_action)d" type="action"
                                    string="Bài viết" class="btn-link" icon="oi-arrow-right"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="action_knowledge_configuration" model="ir.actions.act_window">
        <field name="name">Cài đặt</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="context">{'module': 'knowledge'}</field>
    </record>

    <menuitem id="menu_knowledge_config" name="Cấu hình" parent="menu_knowledge_root" sequence="90"
        groups="knowledge.group_knowledge_manager"/>
    <menuitem id="menu_knowledge_settings" name="Cài đặt" parent="menu_knowledge_config"
        sequence="0" action="action_knowledge_configuration" groups="base.group_system"/>

</odoo>
