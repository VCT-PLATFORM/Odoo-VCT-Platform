<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="group_approval_manager" model="res.groups">
        <field name="name">Phê duyệt: Quản trị</field>
        <field name="category_id" ref="base.module_category_human_resources"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Người dùng chỉ thấy yêu cầu của mình hoặc mình là người duyệt -->
    <record id="approval_request_rule_own" model="ir.rule">
        <field name="name">Yêu cầu: của tôi hoặc tôi duyệt</field>
        <field name="model_id" ref="model_approval_request"/>
        <field name="domain_force">['|', ('request_owner_id', '=', user.id), ('approver_ids.user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
    </record>
    <record id="approval_request_rule_manager" model="ir.rule">
        <field name="name">Yêu cầu: quản trị xem tất cả</field>
        <field name="model_id" ref="model_approval_request"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_approval_manager'))]"/>
    </record>
</odoo>
