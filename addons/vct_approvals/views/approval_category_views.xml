<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="approval_category_view_kanban" model="ir.ui.view">
        <field name="name">approval.category.kanban</field>
        <field name="model">approval.category</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="request_count"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click">
                            <strong><field name="name"/></strong>
                            <div class="text-muted"><field name="request_count"/> yêu cầu</div>
                            <div class="mt-2">
                                <button name="action_new_request" type="object" class="btn btn-primary btn-sm">
                                    Tạo yêu cầu
                                </button>
                                <button name="action_view_requests" type="object" class="btn btn-link btn-sm">
                                    Xem
                                </button>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="approval_category_view_list" model="ir.ui.view">
        <field name="name">approval.category.list</field>
        <field name="model">approval.category</field>
        <field name="arch" type="xml">
            <list string="Loại phê duyệt">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="approval_minimum"/>
                <field name="user_ids" widget="many2many_tags"/>
                <field name="request_count"/>
            </list>
        </field>
    </record>

    <record id="approval_category_view_form" model="ir.ui.view">
        <field name="name">approval.category.form</field>
        <field name="model">approval.category</field>
        <field name="arch" type="xml">
            <form string="Loại phê duyệt">
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="Vd: Duyệt chi phí"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="approval_minimum"/>
                            <field name="has_amount"/>
                            <field name="has_date"/>
                        </group>
                        <group>
                            <field name="active" invisible="1"/>
                            <field name="color" widget="color_picker"/>
                        </group>
                    </group>
                    <separator string="Người duyệt mặc định"/>
                    <field name="user_ids" widget="many2many_tags"/>
                    <separator string="Mô tả"/>
                    <field name="description"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_approval_category" model="ir.actions.act_window">
        <field name="name">Loại phê duyệt</field>
        <field name="res_model">approval.category</field>
        <field name="view_mode">kanban,list,form</field>
    </record>
</odoo>
